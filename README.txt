Brandon Chan

Data from Basketball Reference

To run the code:
Download the code base and install Python libraries. Run python main.py through the command terminal. From there you can train, create and use the model.
